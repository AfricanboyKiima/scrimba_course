favouriteMovieGenre("cowboy")

favouriteFruit("reqular")

favouriteMode("banana")

favouriteStyle("dark")

function setProp(prop, value) {
    document.documentElement.style.setProperty(prop, value)
}